[GENERAL]
WorkingDir=/app/targetpathogenweb
Monitoring=False
MaxWorkers=2
EnvironmentFile = settings.env
[SSH]
; Prefer setting SSH_* via environment (.env / compose). Keep these as fallback-only.
Username=
# Password intentionally blank; use SSH agent with your key
Password=
HostName=
WorkingDir=
Cores=
